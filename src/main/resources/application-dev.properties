# Database:
spring.datasource.url=jdbc:mysql://localhost:3306/myblog?useUnicode=true&characterEncoding=utf-8
spring.datasource.username=root
spring.datasource.password=Cz769337031.
# the line below will ask hibernate to change the database according to the given entity structures.
# if the entity has been well defined, I should change it to 'none' in case of messing the database.
spring.jpa.hibernate.ddl-auto=update
# This will print sql queries to standard output since we need to see those auto-generated SQL queries
# and the order in which they were executed for debugging purposes
spring.jpa.show-sql=true
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Logging part:
logging.level.root = info
# To make spring boot write to a log file
logging.file.path = log/myblog-dev.log
